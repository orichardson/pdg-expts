\documentclass[the-pdg-manual.tex]{subfiles}

\begin{document}
    \section{Statistical Distances and PDGs}


    Suppose we had a PDG that had just two

    \begin{center}
        \begin{tikzpicture}[scale=2]
            %%% DRAW AXES
                \draw[black,very thick, ->] (0,0) -- (3,0) node[right]{$\beta_q$};
                \draw[black,very thick, ->] (0,0) -- (0,3);
                \draw[gray, thick, ->] (0,0) -- (-3,0);
                \draw[gray, thick, ->] (0,0) -- (0,-2);
                % black tick marks
                \foreach \x in {1,...,5} {
                    \draw[black,thick] ($(\x,0) + (0,0.1)$) -- +(0,-0.2); }
                \foreach \y in {1,2} {
                    \draw[black,thick] ($(0,\y) + (0.1,0)$) -- +(-0.2,0); }
                % grey tick marks
                \foreach \x in {1,...,3} {
                    \draw[gray,thick] ($(-\x,0) + (0,0.1)$) -- +(0,-0.2); }
                \foreach \y in {1,2} {
                    \draw[gray,thick] ($(0,-\y) + (0.1,0)$) -- +(-0.2,0); }


            % The y = -x bound
            \draw[very thick, violet!50] (-3,3) -- (3,-3);

        \end{tikzpicture}
    \end{center}


    \subsection{R\'enyi Divergences}
    The R\'enyi divergence of order $\alpha$ of $q(X)$ with respect to $p(X)$ is
    \[ \Div[\alpha] p q = \frac1{\alpha -1}\log \int p(x)^\alpha q(x)^{1-\alpha} \mathrm dx. \]



    \begin{prop}
        For all $\alpha \in (0,1)$, the inconsistency of a PDG containing $p$ and $q$, in which $\beta_p = \frac{\alpha}{1-\alpha}$ and $\beta_q = 1$, is equal to the R\'enyi divergence $\Div[\alpha]p q$.
        That is,

        \[
            \Inc\left(
            % \aar[\Bigg]{
            \begin{tikzpicture}[center base]
                \node[dpad0] (X) {$X$};
                \draw[arr2,<-] (X)
                    -- node[above]{$p$}
                       node[below]{$\scriptstyle\left\{\beta=\frac{\alpha}{1-\alpha} \right\}$}
                     ++(-2,0);
                \draw[arr2,<-] (X)
                    -- node[above]{$q$}
                       % node[below]{$\scriptstyle\{\beta=1 \}$}
                     ++(1,0);
            \end{tikzpicture}
            % \overset{\{\beta=\frac{\alpha}{1-\alpha}\}}p \bundle q
            % }
            \right)
            = \Div[\alpha]p q \]
    \end{prop}

    The R\'enyi divergence is assymetric in its arguments.

    \subsection{}


\end{document}
